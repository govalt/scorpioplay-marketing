# Optional: set this to a credential-free Scorpio Play catalog endpoint.
# Never expose private API keys through NEXT_PUBLIC_ variables.
NEXT_PUBLIC_SCORPIO_CATALOG_URL=

# Server-only SMTP settings used by the API access form.
CONTACT_EMAIL=affiliatemg@scorpioplay.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
